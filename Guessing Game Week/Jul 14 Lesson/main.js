let passwords{
    ""
}

console.log(passwords);


passwords = passwords.split("\n");
passwords.shift();
passwords.pop();
console.log(passwords);

//need a random pw from pw array
//math random is random value between 0 and 1
//math floor keeps it at 100 max

console.log(password[Math.floor(Math.random() * passwords.length)]); //still has decimals
let password = passwords[Math.floor(Math.random()passwords.length)]); //

console.log(passwords[100]);